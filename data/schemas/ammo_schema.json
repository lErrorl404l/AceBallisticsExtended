{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/lErrorl404l/AceBallisticsExtended/schemas/ammo",
  "title": "ABE Ammo Configuration",
  "description": "Ammunition/projectile configuration for the Advanced Ballistics Extension",
  "type": "object",
  "required": ["ammoClass", "projectileMassG", "caliberMm"],
  "properties": {
    "ammoClass": {
      "type": "string",
      "description": "ARMA 3 CfgAmmo class name this config applies to",
      "pattern": "^[a-zA-Z_][a-zA-Z0-9_]*$"
    },
    "projectileMassG": {
      "type": "number",
      "description": "Projectile mass in grams",
      "exclusiveMinimum": 0,
      "maximum": 50000
    },
    "caliberMm": {
      "type": "number",
      "description": "Projectile caliber in millimeters",
      "exclusiveMinimum": 0,
      "maximum": 200
    },
    "ballistic_properties": {
      "type": "object",
      "description": "Interior ballistics parameters for propellant burn rate customization",
      "default": {},
      "properties": {
        "char_length": {
          "type": "number",
          "description": "Mayer-Krause characteristic burn length in meters. Fast pistol powders ~0.15–0.18, medium rifle ~0.25–0.30, slow magnum ~0.35–0.45.",
          "default": 0.28,
          "minimum": 0.05,
          "maximum": 0.60
        },
        "burn_rate_coeff": {
          "type": "number",
          "description": "Burn rate efficiency scale factor. >1.0 = faster burn (higher MV), <1.0 = slower burn (lower MV).",
          "default": 1.0,
          "minimum": 0.1,
          "maximum": 3.0
        }
      },
      "additionalProperties": false
    },
    "cdmId": {
      "type": "string",
      "description": "Drag model curve ID (g1, g7, g8, or custom curve name)",
      "default": "g7"
    },
    "bcG1": {
      "type": "number",
      "description": "Ballistic coefficient (G1 model)",
      "exclusiveMinimum": 0,
      "maximum": 10
    },
    "bcG7": {
      "type": "number",
      "description": "Ballistic coefficient (G7 model)",
      "exclusiveMinimum": 0,
      "maximum": 10
    },
    "bcSupersonicFactor": {
      "type": "number",
      "description": "BC scale factor in supersonic regime (Mach ≥ 1.2). Reference BC is typically measured at supersonic speeds (Mach 1.5-2.5), so this is usually 1.0.",
      "default": 1.0,
      "minimum": 0.1,
      "maximum": 2.0
    },
    "bcSubsonicFactor": {
      "type": "number",
      "description": "BC scale factor in subsonic regime (Mach ≤ 0.8). G7 boat-tail projectiles typically recover to ~1.15× reference BC subsonically. G1 flat-base: ~1.05×.",
      "default": 1.05,
      "minimum": 0.1,
      "maximum": 2.0
    },
    "muzzleVelocityMs": {
      "type": "number",
      "description": "Muzzle velocity in m/s (from reference barrel length)",
      "exclusiveMinimum": 0,
      "maximum": 2500
    },
    "referenceBarrelLengthMm": {
      "type": "number",
      "description": "Barrel length used for muzzleVelocityMs measurement",
      "exclusiveMinimum": 0,
      "maximum": 2000
    },
    "fragThresholdMs": {
      "type": "number",
      "description": "Minimum velocity for fragmentation in m/s (-1 = no fragmentation)",
      "default": -1
    },
    "fragCount": {
      "type": "integer",
      "description": "Expected number of fragments on fragmentation",
      "default": 0,
      "minimum": 0,
      "maximum": 5000
    },
    "fragMassDist": {
      "type": "string",
      "description": "Fragment mass distribution model",
      "enum": ["log_normal", "uniform", "mott"],
      "default": "log_normal"
    },
    "projectileType": {
      "type": "string",
      "description": "Projectile construction type",
      "enum": ["fmj", "soft_point", "hollow_point", "ap", "api", "tracer", "incendiary"],
      "default": "fmj"
    },
    "frag_mass_mean": {
      "type": "number",
      "description": "Mean fragment mass in grams for log-normal distribution",
      "default": 0.0
    },
    "frag_mass_std": {
      "type": "number",
      "description": "Standard deviation of fragment mass in grams",
      "default": 0.0
    },
    "ricochet_angle_deg": {
      "type": "number",
      "description": "Maximum ricochet angle in degrees; 0 uses default calc",
      "default": 0.0
    },
    "tracer_burn_time_s": {
      "type": "number",
      "description": "Tracer burn duration in seconds; 0 = no tracer",
      "default": 0.0
    },
    "incendiary": {
      "type": "boolean",
      "description": "Whether projectile has incendiary effect",
      "default": false
    },
    "incendiary_ignition_temp_k": {
      "type": "number",
      "description": "Ignition temperature of incendiary filler in Kelvin",
      "default": 0.0
    },
    "notes": {
      "type": "string",
      "description": "Free-text notes about data source or assumptions"
    }
  }
}
